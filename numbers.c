/*
Author: Jordan Chute
Program Description: This program prints the numbers from 0 to 9 to the screen
 */
#include <stdio.h>

int main() {

    // Print 0-9 to the screen, a number per line
    for (int i = 0; i < 10; i++) {
        printf("%d\n", i);
    }

    return 0;
}